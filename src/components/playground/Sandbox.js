export default function Sandbox(props) {

    /* let totalPrice = props.price * props.quantity
    const priceDiscount = props.cipela

    if (props.quantity >= 5) {
        totalPrice = totalPrice - totalPrice / 100 * priceDiscount
    }

    if (props.quantity > 7) {
        props.risetajKaunter(1)
    } */


    return (
        <div>
            {/*             <h2>Cijena ovih proizvoda je: {totalPrice}</h2>

            <input type="text" value={props.name} onChange={(e) => props.postaviIme(e.target.value)} placeholder="Upiši ime i prezime" /> <br />

            <input type="text" value={props.adresa} onChange={(e) => props.postaviAdresu(e.target.value)} placeholder="Upiši adresu" /> <br />

            <input type="text" value={props.phone} onChange={(e) => props.postaviBroj(e.target.value)} placeholder="Upiši broj telefona" /> */}






        </div>


    )
}